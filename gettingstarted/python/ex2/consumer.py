# coding=utf8

# connect to WAMP router, join a realm (no authentication), subscribe to (non-XBR, plain WAMP) events
# published in producer, call a procedure (non-XBR, plain WAMP) registered in producer
